import { ExportReviewView } from '@/components/export/ExportReviewView'

export default function ExportReviewPage() {
  return (
    <ExportReviewView
      eyebrow="Outputs"
      title="CSV Export"
      description="Review your calendar time as invoice line items. Edit any field, bulk-update rows, then export to CSV."
    />
  )
}
